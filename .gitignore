# dependencies
node_modules/
.pnpm-store/

# next.js / build
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# env and secrets
.env
.env.*
!.env.example

# tests / tooling
coverage/
playwright-report/
test-results/
.playwright-mcp/

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# local scratch
scratch/
*.log
docs/research/

# Rendered locally for latency measurement (pnpm fixtures:wav:latency); 5 MB, not needed to run the app
tests/fixtures/latency-probe.wav

# ---------------------------------------------------------------------------
# Owner's instruction 2026-09-06: keep GitHub to the project itself. The
# planning specs, the deck and the video live locally and are uploaded to
# lablab directly, so they are not carried in the repo. README.md, LICENSE and
# docs/images stay, because the README needs them and judges read them.
# ---------------------------------------------------------------------------

# planning and operating documents
prd.md
trd.md
CLAUDE.md
PRODUCT.md
DESIGN.md
docs/*.md
docs/plans/
# ...except the two the README links to, so it has no broken links on GitHub
!docs/decisions.md
!docs/deployment.md
!docs/ARCHITECTURE.md

# deck, slides, video and their working directory
projects/
*.pptx
*.pdf
*.mp4
*.webm
*.mov

# agent and tooling state
.impeccable/
.remember/
.claude/

# Rendered locally by pnpm fixtures:wav and pnpm fixtures:wav:customer.
# 8.5 MB of generated speech, regenerate rather than carry.
tests/fixtures/golden-draft.wav
tests/fixtures/judge-solo-customer.wav
# Rendered by pnpm fixtures:wav:omitted for the keyterms-integrity experiment.
tests/fixtures/omitted-draft.wav
