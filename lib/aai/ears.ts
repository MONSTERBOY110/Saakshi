// Phase 1: typed Streaming STT client (connect, params, every server message, UpdateConfiguration,
// ForceEndpoint, Terminate, reconnect with gap logging). Phase 0 uses lib/spike directly.
export const STT_WS_URL = "wss://streaming.assemblyai.com/v3/ws";
